{
  "name": "base-build",
  "version": "1.0.0",
  "private": true,
  "description": "Bot setup including custom and vocality plugins built with typescript",
  "main": "build/index.js",
  "license": "MIT",
  "scripts": {
    "start": "ts-node index.ts",
    "build": "npm run compile",
    "prepare": "npm run compile",
    "compile": "tsc -p .",
    "clean": "gts clean",
    "check": "gts check",
    "fix": "gts fix"
  },
  "dependencies": {
    "@vocality-org/core": "^1.0.0",
    "@vocality-org/music": "^1.0.0",
    "@vocality-org/random": "^1.0.0",
    "@vocality-org/votes": "^1.0.0",
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "@vocality-org/types": "^1.0.0",
    "ts-node": "^8.6.2",
    "typescript": "^3.7.4"
  }
}
