language: node_js
cache:
  directories:
    - node_modules
before_script:
  - sudo apt-get install ffmpeg -y
deploy:
  provider: heroku
  api_key:
    secure: $HEROKU_KEY
  app: bass-bot-app
  on:
    repo: MatthiasHerzog2000/rythm-bot
